# История версий SberAct

Одна строка на версию, новые — сверху. Версию поднимаем в `package.json` и
`electron-app/package.json` (одинаково) перед каждым релизом.

## 2.0.1
- Тестовое обновление с флешки (Windows): проверка канала electron-updater и
  дельта-обновления конвертера.
- Офлайн-сборка и установка под Linux (AppImage + конвертер + модели + tesseract).

## 2.0.0
- Обновлённая версия SberAct: офлайн-установка и обновления с флешки без
  переустановки, конвертер поставляется отдельным каналом.

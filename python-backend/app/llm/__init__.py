# -*- coding: utf-8 -*-
"""LLM-слой: второй независимый способ разбора полей.

Слой ТЕНЕВОЙ. Он ничего не подставляет и ничего не исправляет — только
отмечает расхождения со штатным regex-разбором, чтобы юрист перепроверил
конкретное поле. Это тот же принцип, что уже действует у debtorNameWarning и
fieldQuality.

Модули без зависимости от llama-cpp (её нет в venv бэкенда — инференс живёт в
конвертере-сайдкаре):
  windows  — нарезка прицельных окон по якорям;
  prompts  — построители промптов и маркеры утечки few-shot;
  parsing  — разбор ответа и детерминированные предохранители;
  matching — сравнение значения LLM со значением regex;
  fields   — раскладка «поле схемы → ключ поля на форме».
"""

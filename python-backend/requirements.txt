fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
python-docx==1.1.0
pypdf>=4.0.0
spacy==3.7.2
numpy>=1.24.0
python-dateutil>=2.8.2
pydantic>=2.0.0
pywebview>=4.0
natasha>=1.6.0
pymorphy3>=1.2.0
# natasha тянет pymorphy2, а тот импортирует pkg_resources, удалённый из
# setuptools>=81. Без этого пина на чистой сборке natasha падает при инициализации
# экстракторов (AddrExtractor/DatesExtractor). Держим setuptools<81, пока natasha
# не перейдёт на importlib.metadata.
setuptools<81

# Прокси /convert/* к OCR-конвертеру (sidecar на 127.0.0.1:8008) — см.
# docs/converter_integration_plan.md.
httpx>=0.27

# --- dev-инструменты (линт/формат, в проде не нужны) ---
ruff>=0.15

# --- shadow-слой семантической классификации (план `new_asnaliz`, ОПЦИОНАЛЬНО) ---
# semantic_classifier.py импортирует эти пакеты ЛЕНИВО (внутри функции, как
# nlp_natasha.py) — их отсутствие не ломает основной анализ, флаг
# SBERACT_SEMANTIC_SHADOW по умолчанию выключен. Ставить только для измерения
# (см. tests/measure_semantic_classification.py): модель
# paraphrase-multilingual-MiniLM-L12-v2 нужно скачать заранее в
# python-backend/models/ — рантайм работает офлайн (local_files_only=True).
#
# ВЕРСИИ ЗАКРЕПЛЕНЫ НАМЕРЕННО: обычный `pip install sentence-transformers`
# тянет свежие scikit-learn/scipy, которым нужен numpy>=2.0 — а spacy==3.7.2/
# thinc в проекте требуют numpy<2.0 (без пина падает импорт spacy с
# «numpy.dtype size changed, may indicate binary incompatibility», проверено
# на этой машине). Эти версии scikit-learn/scipy/sentence-transformers
# совместимы с numpy 1.26.4 — держим их, пока spacy не перейдёт на numpy 2.
sentence-transformers==2.7.0
scikit-learn<1.5
scipy<1.13
torch>=2.0
